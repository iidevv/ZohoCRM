{% if this.entity.decodedZohoErrors %}
    {% set errors = this.entity.decodedZohoErrors %}
    {% if errors.message %}
        {{ errors|json_encode|e('html_attr') }}
    {% endif %}
{% else %}
    -
{% endif %}